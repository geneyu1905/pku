#include <iostream>
#include <iomanip>
using namespace std;
int main()
{
	double fact = 1;
	double sum = 1;
	for (int i = 1; i < 10; i++)
	{
		fact *= i;
		sum = sum + (1 / fact);
	}
	cout << fixed << setprecision(6) << sum << endl;
}

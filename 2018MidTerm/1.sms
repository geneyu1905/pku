#include <iostream>
using namespace std;
int main()
{
	int n = 0;
	int number = 0;
	double sum = 0;
	cin >> n;
	for (int i = 0; i < n; i++)
	{
		cin >> number;
		while (number > 0)
		{
			sum += 0.1;
			number -= 70;
		}
	}
	cout << sum << endl;
}
